// Legacy file retained to avoid breaking imports. Prefer using src/content/testimonials.
export type Review = never;
export const reviews: never[] = [];
export function getDedupedReviews(): never[] { return []; }


